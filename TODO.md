# TODO

## Roadmap

- Implement TypeScript across the whole repo (client, server, shared config)
- Review the testing setup (Vitest, coverage, CI, fixtures)
- Review naming and copy in components and make any necessary adjustments
- Upgrade React to the latest React 19 release and bump related libraries (react-dom, types, router, testing libs, etc.)

## Recommendations (assistant suggestions for GitHub / profile polish)

These are optional follow-ups suggested by the assistant to make the repo read well on your GitHub profile—not part of your original roadmap above.

- **README:** Add a one-line pitch, stack list, and local setup (Node version / `.nvmrc`, install steps, env vars, dev URLs)
- **README:** Add screenshots or a short GIF; add a live demo link or state that it’s local-only
- **README:** Keep course attribution; optionally add a sentence on what you added or changed beyond the course
- **Repo hygiene:** Decide whether `server/pizza.sqlite` is tracked or gitignored; document how to get a DB on a fresh clone
- **CI:** Add a GitHub Actions workflow (e.g. `npm ci`, lint, test on push/PR)
- **GitHub:** Set repo description and topics (`react`, `vite`, `vitest`, etc.); pin the repo on your profile if it’s a flagship project
